# AGENTS.md

## Cursor Cloud specific instructions

- **Terrain-Dash** is currently an empty/skeleton repository with only a `README.md`.
- There are no source files, dependency manifests, build scripts, or infrastructure configs as of the initial commit.
- No services to start, no tests to run, no build commands available.
- Once application code is added, update this section with relevant dev commands (lint, test, build, run).
